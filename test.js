// 12 e2 00 bb
/*  const buffer = new ArrayBuffer(8);
const uint16 = new Uint16Array(buffer);
console.log(uint16);
console.log(buffer);
console.log(toHexString("12 e2 00 bb"));

function toHexString(byteArray) {
  return Array.from(byteArray, function (byte) {
    return ("0" + (byte & 0xff).toString(16)).slice(-2);
  }).join("");
} */

/* var options = {
  root: null,
  rootMargin: "0px",
  threshold: 1.0,
}; */
// root cible l'ancétre
// margin c'estt quand on a root et margin comme le css + %
// threshold 0-1 % de visibilité de la div
/* const observer = new IntersectionObserver(callback, options);

function callback() {
  console.log("coucou");
}
const ul = document.querySelector("section ul");
console.log(ul);
observer.observe(ul);
 */
